# ⚠️ SECURITY NOTICE - DEMO APPLICATION ⚠️
# This application uses client-side authentication for demo purposes only.
# For production, implement proper backend authentication with:
# - Secure API endpoints (HTTPS)
# - Password hashing (bcrypt, argon2)
# - JWT tokens or secure session cookies
# - Environment-based secrets management
# - Never commit real credentials to version control

# API Configuration
VITE_API_BASE_URL=http://localhost:5000/api

# Data Mode Configuration
# Set to 'false' to use real API, 'true' or omit to use mock data (default: true)
VITE_USE_MOCK_DATA=true

# Application Configuration
VITE_APP_NAME=AI Case Management System
VITE_ENABLE_ANALYTICS=false
